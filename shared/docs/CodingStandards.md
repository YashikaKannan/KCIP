# KCIP Coding Standards

1. Use Modern ES Modules (`export const`, `import`).
2. Constants must use `UPPER_SNAKE_CASE` and be wrapped with `Object.freeze()`.
3. Functions must be camelCase; Classes, Types, Mappers, and Interfaces must be PascalCase.
4. JSDoc header block required on all files.
