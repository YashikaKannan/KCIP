# App configuration
APP_NAME=KCIP API
API_PREFIX=/api
ENVIRONMENT=production
VERSION=1.0.0
HOST=0.0.0.0
PORT=9000
LOG_LEVEL=info

# CORS configuration: comma-separated allowed origins
CORS_ORIGINS=

# Authentication / JWT settings
JWT_SECRET=changeme
JWT_ISSUER=kcip-mock-auth
JWT_AUDIENCE=kcip-frontend
ACCESS_TOKEN_MINUTES=480
